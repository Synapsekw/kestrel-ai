---
type: session
date: 2026-09-21-1753
branch: main
trigger: wrapup
status: complete
tags: [session]
related: ["[[2026-09-21-gotcha-ui-captures-need-real-image-bounds]]"]
---

# Contour UI

## What changed

- The operator chose Contour from the standalone design study. Implementation ran in
  `.claude/worktrees/contour-ui`, branch `task/contour-ui`.
- `4af3b5d` records the spec/plan; `275f6fe` implements the charcoal/amber system, compact expandable
  navigation, bounded Home previews and single image inspector. `bd145d4` fixes clipped help,
  initial shortcut-dialog focus and hero resolution. `3e82cc1` records browser tests/screenshots;
  `50c5e0d` records the final review and integration state.
- Task review, fix review and final whole-branch review passed. The final review found no remaining
  critical, important or new minor issues. Rebase retained the concurrent folder-rename preparation
  at `9fc1429`; frontend/backend/contract tree hashes matched the tested state exactly afterward.
- Gate: contract check, Ruff, 620 backend tests, frontend lint, 517 tests across 122 files, build and
  57 browser tests. Rust was skipped because the fresh worktree had no frozen sidecar. A first
  backend run failed one intermediate-epoch timing assertion; isolated 12/12 and full 620/620 reruns
  passed without backend source changes. Existing test/build warnings are recorded in the evidence.
- `finish-task.ps1 -SkipGate` reused those completed checks, fast-forwarded and pushed main from
  `9fc1429` to `50c5e0d`, removed the worktree registration and deleted the task branch. Its residual
  empty directory was verified and removed separately. Shared Python remains intact: torch
  `2.14.0+cu130`, CUDA available.

## Why

The selected visual direction needed to become a useful working interface, with more canvas space,
clearer review controls and readable dark-theme states. The existing annotation and job workflows
remain intact; the mockup's illustrative statistics were not copied into production.

## Open threads

- The installed desktop executable was not rebuilt or replaced. Browser evidence uses real React
  screens with Prism metadata and a local photograph; it is not installed-app acceptance.
- Existing React Router/mock Konva warnings and the bundle-size warning remain. No unrelated warning
  cleanup or backend timing refactor was included.
- The folder rename and existing OBB/acceptance backlog remain separate tasks.

## How to test

1. Start the updated development app and open a project. Check Home's next action and image preview.
2. Expand/collapse navigation. Focus a locked step in a project without its prerequisite and confirm
   its explanation is visible beside the rail.
3. Browse/filter/select Images, then open Label. Change Drawing class, expand All classes, review a
   suggestion and use undo/redo. Open Keyboard shortcuts and confirm it starts at the top.
4. Resize to 1024×768. Confirm useful canvas area, scrolling inspector and readable help. Open Jobs
   and settings; verify focus rings and reduced motion.

## Next session entry point

`docs/evidence/ui/2026-09-21-contour/README.md` contains screenshots, fixture provenance, gates and
reproduction steps. Build/install from current main and run the installed walkthrough if the operator
requests the desktop update. For further UI work read the updated `DESIGN.md` and `PRODUCT.md`.
