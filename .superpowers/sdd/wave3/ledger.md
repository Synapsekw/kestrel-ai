# SDD ledger — plan: docs/superpowers/plans/2026-09-18-s6-packaging-acceptance.md (Wave 3, S6)

Checkpoint 3 passed on main 6635f71 (docs 958ece8). Goal-owner follow-ups landed before dispatch: JobCancelled leaf module (e85a363), Train form keeps typed values (024ec6f), contract Health.gpu optional block (f52267c). Worktree .worktrees/s6-packaging-acceptance at f52267c; node modules installed by the goal owner; backend uses the shared venv.

Pre-flight scan (plan vs itself and the global constraints):

| Pair / task | Produces vs consumes | Finding |
|---|---|---|
| T1 spec/build.ps1 vs T3 tauri build | T1 dist/machinery-backend copied into frontend/src-tauri/binaries; T3 bundles it as the sidecar plus `_internal` resources | consistent; T3 tells the implementer to fix `bundle.resources` if `_internal` lands elsewhere |
| T1 health.gpu vs contract | T1 says "goal owner edits the contract; ask before you change it" | resolved: Health.gpu added by the goal owner (f52267c) before dispatch; implementer only fills it |
| T2 sidecar log vs T3 timing / T5 README | T2 writes <app_data>/logs/sidecar.log and returns log_path; T3 reads it for the health timestamp; T5 documents the path | consistent |
| T2 orphan sweep vs S4/S5 job semantics | queued -> cancelled, running -> failed "interrupted by application restart" | consistent with the Job state enum in the contract |
| T4 acceptance driver vs T3 installed app | driver attaches over CDP to the installed app | see ruling 1: the implementer dry-runs it against `pnpm tauri dev` in env mode; the goal owner runs it on the installed app |
| T1 console=False | plan allows keeping console=True with a note | implementer's call; must be recorded in the report |
| Each task's own text | tests named for the code they cover; build steps verified by recorded script output | no contradiction found |

Ruling 1: verification stays with the goal owner. The implementer builds the installer (Task 3 build part) and records size and build time, writes the acceptance script and driver (Task 4) and dry-runs the driver against the dev app in env mode with a temp project (import from a copy of the sample frames, never from the original imagery), but does NOT install the app, time the cold start or run the acceptance flow on the installed app; those are checkpoint 4 and the acceptance run, done by the goal owner after review. Cost if wrong: one extra driver run by the goal owner.
Ruling 2: the acceptance import (3299 frames) reads `E:\Dev\Yolo\Ahmadia Construction Data` read-only per spec 13.5; the driver must never write there, and the sub-agent's dry run uses `E:\Dev\Yolo\data\raw\ahmadia` copies instead. Cost if wrong: none (read-only source either way).
Ruling 3: the acceptance driver's Anthropic step reads ANTHROPIC_API_KEY from the environment at runtime and skips with a clear message when absent (same as checkpoint 3); the key is never written anywhere.
S6 implementer dispatched (opus) in .worktrees/s6-packaging-acceptance at f52267c (BASE f52267c); briefs s6-task-1..5-brief.md; report s6-report.md. Runs in the background; the goal owner waits for the completion notification.
S6 implementer: BLOCKED on Task 3 after tasks 1, 2, 4, 5 (eb3189b..02359a9): NSIS and MSI cap payloads at 2 GB; sidecar 3.46 GB.
  Ruling: Inno Setup 6 via the innosetup-compiler npm package (local node_modules), wrapping tauri build --no-bundle output; per-user install; WebView2 bootstrapper run when the runtime is missing; spec section 10 amended (decision 13). Cost if wrong: an installer format outside the Tauri bundler to maintain; no updater plugin integration.
S6 task review dispatched (fable) on f52267c..02359a9 while the implementer resumes Task 3.
Goal-owner verification on s6 branch (02359a9, backend only while the implementer works on the installer): ruff clean, 401 passed (matches the report). Frontend suites and gpu tests to be re-run after the Task 3 commit.
S6 review (fable): spec T1 ✅ T2 ✅ T4 ❌ T5 ✅; 1 Critical (virtualised table: 30-row selection impossible by label), 4 Important (I1 pre-annotate race, I2 overwrites operator key, I3 no cloud provenance assertion, I4 md/driver disagree on progress events), 14 Minor. Review at .superpowers/sdd/wave3/s6-review.md.
S6 fix round 1/5 queued to the implementer (after Task 3): C1, I1-I4, minors M1-M8, M10, M12-M14.
  Deferred minors (S6 round 1): M9 only the worker redirects the Ultralytics config dir (document); M11 sidecar log rotation when the rename fails keeps growing until released.
S6 Task 3 done (b96c6d9: Inno Setup installer 1,797 MB in 377 s; sidecar installed as machinery-backend.exe without target triple since the shell plugin resolves <exe dir>\<name>.exe; no WebView2 bootstrapper available offline) and fix round 1 (6cdd435; dry run 30 labeled / 50 query on 90 frames: 7 passed, 1 skipped). Review of Task 3 + scoped re-review of round 1 dispatched (opus); goal owner re-running all suites.
Goal-owner verification on s6 6cdd435 (frontend): lint clean, 209 unit, build ok, 42 e2e. Backend/gpu/contract run in progress.
Goal-owner verification on s6 6cdd435 (backend): ruff clean, 403 passed, gpu 4 passed, contract check clean. All suites match the report; merge waits on the Task 3 review + round 1 re-review.
S6 Task 3 review + round 1 re-review (opus): installer spec ✅ (sidecar name resolution and bundle.targets [] verified against tauri-plugin-shell 2.3.6 / tauri-build 2.6.3); round 1: 15 addressed, 2 partial (M3, M14), 0 not; Important A1 (README names the wrong installed sidecar), N1 (selectRows does not confirm the filtered list); 12 minors. Verdict: approved with fixes.
S6 fix round 2/5 dispatched: A1, N1, A3, A4, A6, A7, A8, N2-N6; installer rebuilt after the .iss changes.
  Deferred minors (S6 round 2): A2 installer sources from src-tauri/binaries with no freshness check against backend/dist (README note only); A5 WebView2 bootstrapper copied into frontend/installer (gitignored).
S6 fix round 2 (9f3aa01): implementer DONE; installer rebuilt (1,797.3 MB, 387 s); 90-frame dry run 7 passed / 1 skipped with 30 labeled and 50 query images verified against the API. Scoped re-review dispatched (sonnet); goal owner re-running all suites.
Goal-owner verification on s6 9f3aa01: ruff clean, 403 backend, contract check clean, frontend lint clean, 209 unit, build ok, 42 e2e (gpu 4 passed at 6cdd435; round 2 touched no gpu code). Merge waits on the round 2 re-review.
S6 round 2 re-review (sonnet): 12/12 addressed, no new defects, approved. Merged to main 826a3bf (progress.md conflict resolved by hand). Installed the setup exe built at 9f3aa01 (/VERYSILENT /CURRENTUSER): exit 0 in 60 s, install dir 3,512 MB (machinery-app.exe, machinery-backend.exe, _internal, unins000), Start Menu shortcut present. Post-merge suites on main running; checkpoint 4 driver runs once the machine is quiet.
Checkpoint 4 PASS (c835831). Acceptance run on the installed app: steps 1-2 passed (3299 images, 0 duplicates, 7 flights); step 3 failed with the driver opening the first 10 frames of flight 0031 (take-off run-in, empty). Goal-owner experiment: yolo11m at imgsz 2560 / conf 0.25 fires on 49 of 100 frames spread across the import, so the criterion is achievable; driver changed to open 10 evenly spread frames (acceptance.md step 3 updated); resumed with --project-id: step 3 PASS with 4 proposals.
Acceptance run (spec 13.5) on the installed app, project eb32cbef (folder %TEMP%\acceptance-project): steps 1-5 and 8 PASS with the spec's values; step 6 first failed the driver's own minimum of 1 box (spec has none): the 3-epoch model on placeholder labels never exceeds confidence 0.0016 (verified directly with ultralytics at conf 0.001: 5984 boxes on 20 frames, max 0.00164), so the run was repeated at conf 0.001: 54,670 boxes over 50 images, 50 images in the review queue, promoted. Driver fix: the review queue row count was read while the list was still loading (now waits). Step 7 SKIPPED: no ANTHROPIC_API_KEY (operator-provided). Five driver invocations in total (run1 import cut by the tool's 10 min cap; runs resumed with --project-id). Evidence docs/evidence/acceptance.
Uninstall check and reinstall from dist/ in progress; then docs and commits.
Uninstall check: unins000.exe /VERYSILENT exit 0 in 7 s; install dir and Start Menu shortcut removed; app data (logs, recent_projects.json, ultralytics) kept. Reinstalled from dist/ (exit 0, 58 s) so the installed app remains the deliverable. S6 worktree removed. Acceptance recorded in docs/progress.md; this ledger copied to docs/superpowers/plans/2026-09-18-wave3-ledger.md.
Incident: rm -rf of the S6 worktree followed a junction into backend/.venv and emptied it; venv recreated from requirements-lock.txt (uv, cu130 index) and verified (torch 2.14.0+cu130 CUDA True, ultralytics 8.4.154). Rule recorded in docs/progress.md.
Post-acceptance polish (goal owner, TDD): job.progress message now carries loss terms and ETA (spec 7); TrainProgress shows Loss and ETA tiles.
