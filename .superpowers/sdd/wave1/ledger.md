# Wave 1 ledger (S1, S2, S3)

S3 implementer (opus): DONE_WITH_CONCERNS, 7 commits b233a82..4f036d0; 155 tests; GPU test passed (1-epoch YOLO11n).
Goal owner: installed onnx 1.22.0, onnxslim 0.1.96, onnxruntime 1.30.0 into the shared venv (commit 6082e43); re-ran `pytest -m gpu` in the S3 worktree: 1 passed in 26 s with the real ONNX export assertions.
Contract edits for S3 gaps (4ef79e1): ModelImport.weights_path minLength 1; ExportRequest.half description (onnx on CPU, engine on GPU 0).
S3 review (fable): spec ✅, quality Needs fixes. Important: freeze_support missing in __main__ (frozen dataloader workers would start the API); amp="bf16" unconditional (fails on pre-Ampere GPUs); 422 for bad weights_path now allowed by the contract.
S3 fix round 1/5 dispatched (findings 1-3 + minors: dataset class-order check, TensorRT onnx intermediate recorded, decisive cancel test, params read inside try).
  Deferred minors: POSIX branch lacks killpg (Windows-only product); delete_model leaves runs/<id>/train/weights checkpoints; relative() falls back to absolute; Arial.ttf download on first plot (packaging: pre-seed the font, S6); UI only sees epoch/mAP50 in job.progress (loss/ETA in progress.jsonl and job log; spec 7 says "epoch, loss terms, mAP and ETA" -> S5 may need a richer message: Ruling: put loss and ETA into the job.progress message text in S5 via the job log or extend the message string in a later fix; cost if wrong: UI shows less detail); GPU job concurrency (two worker threads): Ruling: add a per-device lock in S4 when inference jobs also use the GPU; cost if wrong: two trainings compete for VRAM.
S2 plan written (fable, retry after a stalled first attempt): 20 tasks, 6697 lines. Contract gaps -> Rulings: (1) added `unreview` to BoxReview.action (contract d30fa90) so undo of accept/reject restores unreviewed; S1 notified to implement it; cost if wrong: one enum value unused. (2) Delete on a proposal = reject (undo = unreview); DELETE only for person boxes; cost if wrong: users cannot hard-delete proposals (they can reject). (3)(4) client-side filters for box count/capture time and free-text group filter stand.
S2 implementer dispatched (fable, per kickoff: annotation canvas = most capable model).
S3 fix round 1/5 (commits 4f036d0..1c385b2, plus cherry-pick 5c6d9ed of contract 4ef79e1): implementer reports 1,2,4,5,6,7 done; 3 half (422 only for empty path). Ruling: relative/missing weights_path answers 404 not_found (schemathesis positive_data_acceptance forbids 422 on schema-valid strings); contract description updated (0fd7e3d); pyyaml made explicit in requirements.txt. Re-review dispatched (opus).
S3 re-review (opus): 7/7 addressed, no new Important. S3 complete (b233a82..1c385b2). Merged to main 1224343 (contract conflict resolved with main's wording; client regenerated). main verification: backend 168 passed, ruff clean, contract check clean, GPU test passed (1-epoch train + real ONNX export).
  Deferred minors: worker amp TypeError fallback path uncovered (dead against pinned torch); corrupt data.yaml -> 500; strict order-sensitive class drift check (S1 coupling, intended); Dataset.classes assumed list of dicts with name.
INCIDENT 2026-09-18 ~07:03: shared backend venv lost package directories (fastapi, anyio, cv2, dotenv, ...) while dist-info survived. Most likely a git clean of ignored files in a worktree following the backend/.venv junction (S1 report mentions clearing .hypothesis). Also a stray dev backend from S1's smoke test held pydantic_core locked; stopped by PID. Recovery: uv pip install --reinstall from requirements-lock.txt. Ruling: junctions removed; sub-agents use the absolute interpreter E:\Dev\Yolo\app\backend\.venv\Scripts\python.exe with cwd in their worktree; never git clean. Cost if wrong: none.
S1 implementer (opus): DONE_WITH_CONCERNS, 12 commits b233a82..87b85b3, 170 tests. Contract follow-ups by goal owner (5ce69c9): ClassDefInput.name pattern \S; search wording; group_key examples "0031".
S1 review (fable): spec ❌ Critical: materialise flattens images/<site>/<name> -> silent overwrite across sites. Important: same-site second source collides on ix_image_path; PATCH box with explicit null -> 500.
S1 fix round 1/5 dispatched (1-3 + minors 4-8: exif fallback, duplicate skip seeding, Source.image_count after delete, dataset row cleanup on failure, atomic derived-file writes).
  Deferred minors: dataset_stats.groups shows group_key for by_tile datasets; re-posting a folder ignores a new site and resets settings; no Image.draft fast decode for thumbnails (first grid load slow); rglob on a vanished folder -> raw error; cursor tests only for path desc; no resume-after-cancel test; test_cancel_import timing-dependent.
S1 fix round 1/5 (commit 22482d3): all 8 findings claimed fixed, 179 tests; implementer states it never ran git clean (only rm -rf .hypothesis); venv damage cause remains unattributed. Re-review dispatched (opus).
S1 re-review (opus): 8/8 addressed. S1 complete (b233a82..22482d3). Merged to main (conflicts in conftest.py: kept both fixture sets, eight-class `project`; test_contract.py: EXPECTED_STUBS now the 11 S4 operations). main: 250 backend tests passed, ruff clean.
  Deferred minors (S1 round 1 re-review): same-site concurrent imports can still collide (snapshot at job start; Ruling: serialise import jobs per project later or in S6 hardening); pre-upgrade .duplicates.json entries lack source_id (greenfield install, ignore); queued-then-cancelled dataset job never runs discard (name burnt); rmtree failure loop after _place raise; discard exception masks the original error; os.replace PermissionError window on Windows while a FileResponse is open; stray .tmp not matched by bulk_delete glob; BoxUpdate app-schema default null; Source.duplicate_count is per site; dedupe scoped per source; .duplicates.json never pruned.
S2 implementer (fable): DONE, 22 commits d30fa90..b9f51ba, 95 unit + 24 e2e (claimed). Review dispatched (fable); goal owner re-running lint/test/build/e2e independently.
S2 verification by goal owner in the worktree: lint clean, 95 unit, build ok, 24 e2e, no leftover servers.
S2 review (fable): spec ✅; Critical: click-to-deselect creates a 2x2 box (plan-mandated code). Important: undo/redo not re-entrant; redo after re-create PATCHes a dead id; stale command responses applied to the next image; Konva node desync on failed PATCH; space-pan cannot start over a box; history order = response order.
  Ruling: plan-mandated defects are fixed anyway (spec is the authority; undo/redo must be reliable for ground truth). Cost if wrong: none.
S2 fix round 1/5 dispatched (findings 1-7 + minors: next/prev live during load, preannotation busy guard, Number("") guard, ARIA fixes).
  Deferred minors: 409 class_in_use resets other draft edits; useImageList error path keeps old cursor; images.changed reloads first page only (drops loadMore rows/selection); waitForIdle navigates after 10 s silently; re-created box moves to end of region list; nav context capped at 1000 ids; no unit tests for useEditorImage/useEditorHotkeys (e2e only).
S2 fix round 1/5 (commit 75fb5ef): implementer claims 11/11 fixed, 105 unit + 27 e2e. Re-review dispatched (opus); goal owner re-running suites.
S2 fix round 1 re-review (opus): 11/11 addressed; new Important: ImageGrid role="option" nests a checkbox. Round 2/5 dispatched (grid roles, draw anchor in image px, repeat filter for next/prev, catch on syncNode promise).
  Deferred minors: pending counter can be cancelled out across an image change while queued work drains; no request timeouts (a hung request stalls the per-image queue; recoverable by re-navigating); syncNode may snap a box mid second drag; Space mid-drag commits the half move; Keys button accessible name differs from visible text; region rows are all tab stops (roving tabindex later); off-image drags still yield an edge box (clampRect); waitForIdle 10 s timeout; alias map entries outlive the 100-entry history limit; ImageTable grid lacks aria-colcount/rowindex.
S2 round 2 re-review (sonnet): 2,3,4 addressed; 1 markup correct, test missing. Round 3/5 dispatched (ImageGrid role test + catch on drawBox). Goal owner re-ran suites after round 2: 107 unit, 27 e2e.
S2 round 3 re-review (sonnet): addressed. S2 complete (d30fa90..f64d9b7, 25 commits, review clean after 3 rounds). Merged to main 9ed2fd4 (no conflicts). main: backend 252 passed, contract check clean; frontend suites running.
Gap found by goal owner: no "Import images" UI exists (S2 plan did not include it; spec V1 scope 1). Ruling: added to S5's plan (Data Manager toolbar action + Sources section). Cost if wrong: S5 grows by one task.
