--extra-index-url https://download.pytorch.org/whl/cu130
# ML stack: pinned to the reference machine (E:\Dev\Yolo\README.md). Do not change.
torch==2.14.0+cu130
torchvision==0.29.0+cu130
ultralytics==8.4.154
opencv-python==5.0.0.93
ImageHash==4.3.2
numpy==2.4.6
pillow==12.3.0
sahi==0.12.6
# app libraries (exact versions in requirements-lock.txt)
fastapi
uvicorn[standard]
pydantic==2.13.5
pydantic-settings
sqlalchemy>=2.0,<3
alembic
httpx==0.28.1
openai==1.109.1
anthropic
keyring
python-multipart
websockets
piexif
pyyaml
# export formats (spec 7): onnx; tensorrt is added in S6 if a cu13 wheel is available
onnx
onnxslim
onnxruntime
# maps (spec 2026-09-22-geotiff-maps): GDAL/PROJ come bundled in these wheels
rasterio==1.4.4
pyproj==3.7.2
# detection export (plan 2 unit E): the PDF report per source; BSD, pure Python plus pillow
reportlab==5.0.1
# point clouds, volumes, design surfaces (specs 2026-09-23, foundation F0). scipy, shapely and
# psutil were already transitive; they are direct now, at the same versions.
laspy==2.7.0
lazrs==0.8.2
scipy==1.17.1
shapely==2.1.2
psutil==7.2.2
openpyxl==3.1.5
ezdxf==1.4.4
